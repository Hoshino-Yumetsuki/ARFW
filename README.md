# ARFW | APFS Read Only Support For Windows
You need to install WinFsp before using it

- Use `arfw install` to install the application as the windows service.
- Use `arfw uninstall` to uninstall the the arfw windows service.